# ------------------------------------------------------------
# 当前阶段（9-10月：搭数据底座）
# ------------------------------------------------------------
# 统一免费数据源（2026-08-31 决定）：行情 / 估值 / 财务全部走 AkShare，
# 完全免费、无需注册、无积分，且口径统一、下游清洗代码不用改。
# 见 src/fetch_akshare.py。
akshare>=1.18.0
pandas>=2.0.0,<3.0       # 锁 2.x：pandas 3.0 改动大，周边库兼容性还在跟进
numpy>=1.24.0
python-dotenv>=1.0.0     # 读取 .env 里的密钥（Tushare 备用时用到）

# Tushare 仅作「可选交叉校验」备份保留，默认不跑。
# 注意：其免费档（120 积分）只能取日线行情(daily)；市值/PE/PB/财务/指数
# 等接口需要 2000 积分（实质付费）。所以本项目主数据全部用 AkShare，
# Tushare 仅在你想做交叉验证时再用（src/fetch_tushare.py）。
# tushare>=1.2.89          # 可选备份，默认不安装/不运行

# ------------------------------------------------------------
# 后面阶段会用到，先不装也行，到时候再 pip install
# ------------------------------------------------------------
# requests>=2.31.0       # 10月：爬巨潮资讯网
# pdfplumber>=0.10.0     # 12月：解析 PDF 报告
# jieba>=0.42.1          # 12月：中文分词
# scikit-learn>=1.3.0    # 12月：TF-IDF
# statsmodels>=0.14.0    # 2月：回归与事件研究
# linearmodels>=4.31     # 3月：面板固定效应
# matplotlib>=3.7.0      # 全程：画图
# streamlit>=1.28.0      # 6月：最后的网页应用
