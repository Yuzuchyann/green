# week02 · 2026-09-07 ~ · 第 2 周（工作草稿 · 流水账，周五晚整理）

> 本周主线延续 week01：**搭数据底座 · 全市场行情**。本文件是增量草稿，随进度随手记，周五晚统一整理成干净周报。

---

## 2026-09-07（周一）

### 续跑全市场行情（启动）

- 续跑 `fetch_akshare.py --only price`（后台任务 DjUeQl，日志 `data/processed/pull_price_full.log`）。启动前快照：daily 覆盖 3,314 / 5,553（59.6%），910 万行中的 601 万已落库。
- **关键决策：跑完全市场 5553 只**（宝贝原话「跑完全市场」）。
  - 取舍讨论：姐姐先查了计划书——研究核心样本其实只需**已披露公司 ~2720 家**（计划书第七节：披露率 49%；局限 2 自陈「样本仅覆盖已披露公司，存在自选择偏差」），全市场并非科学必需。
  - 最终选全市场的理由：省后续补爬；做 PSM 匹配、全样本稳健性对比、剔除金融业/ST 时「池子」更顺手。是便利性选择，不是科学必需——这点想清楚了。
- 约定完成链（行情跑完后自动执行）：① 校验覆盖是否 5553 全 → ② `git push all`（Gitee+GitHub 双远程）→ ③ 启动全市场财务 `--only fina`。

### ⚠️ 数据质量发现：daily_basic 字段大面积为空（重要，务必写进论文）

- 抽样发现 `daily_basic` 的 **PE / PE_ttm / 流通市值(circ_mv) 大面积为空**——AkShare 免费接口字段返回不全，连 PB、total_mv 也有缺失日。不是我们数据丢了，是接口特性。
- 对策：市值规模控制变量用相对完整的 `total_mv`（万元），或「收盘价 × 总股本」自算；**不可直接依赖 pe_ttm / circ_mv**。
- 同理既有两条：balancesheet 接口坏已绕开（Size 用 total_mv 代理）；AkShare 不给财报真实公布日 → 按报告期对齐（前视偏差，论文须坦白）。

### 停机（23:29 左右，宝贝要关机交电脑）

- 应要求停止。诊断发现：底层 python 子进程已先于外层任务死亡（沙箱回收，日志停于 3309/5553 ≈ 59.6%），外层 Bash 壳 DjUeQl 仍挂 `running` 空状态（与 9/2 同现象，壳没退、子进程已死）。
- TaskStop 清理外层空壳 → Status: killed。
- 停机体检：`integrity_check: ok`；daily 覆盖 **3,314 / 5,553（59.6%，601 万行）**，daily_basic 3,313。
- 进度实时落库，关机无紊乱风险。git push 按约定留待行情全跑完（未做）。

### 9/7 快照

| 项目 | 数量 |
| - | - |
| daily 覆盖 | 3,314 / 5,553（59.6%） |
| daily + daily_basic 累计 | 6,016,075 行 |
| fina_indicator | 5,759 行 / 50 只（未动） |

---

## 2026-09-08（周二）

### 续跑全市场行情（大幅推进）

- 19:52 续跑 `fetch_akshare.py --only price`（后台任务 mf6KR2）。启动前确认：venv akshare 1.18.94 / pandas 3.0.5 正常，daily 3,314 只、integrity ok，git 工作区干净。
- 连续跑了 **3 小时 5 分**（19:52 → 22:57 停），行情从 3,314 → **5,207 / 5,553（93.8%）**，一夜推进约 1,900 只、日线 +300 万行。速度明显快于前几晚（可能是网络时段差异）。
- 22:53 宝贝要求停止 → TaskStop 干净终止（任务状态显示 failed = 主动 kill，非程序出错）。停机体检 `integrity_check: ok`，系统查无 python 进程，可安全关机。

### 9/8 停机后对账（实测）

| 表 | 行数 | 覆盖 |
| - | - | - |
| stocks | 5,553 | 全市场 |
| daily | 9,104,558 | **5,207 / 5,553（93.8%）** |
| daily_basic | 9,104,537 | 同上 |
| fina_indicator | 5,759 | 50 只（试点，未动） |
| balancesheet | 0 | 接口仍坏 |

- daily 日期范围：20180102 – 20260908（含当日）。
- **剩余约 346 只**（93.8% → 100%），下次续跑 `--only price` 自动跳过已搬，零重复，预计几分钟到半小时。

### 插曲：WorkBuddy 工具接口故障（约 22:50–22:56）

- 所有工具调用报 `Parameter expected string, but received undefined`（参数传不到执行层的平台侧故障），连 `echo hi` 都失败。
- 宝贝 `Ctrl + Shift + R` 强刷页面后恢复。**经验**：遇到工具集体报错，先测 `echo hi`；不行就刷新页面/重开会话，不必干等。
- 故障期间数据无任何风险（每搬一只实时落库），停进程延后到恢复后完成。

### 周报整理

- 应宝贝要求，把 week01 周报从「按天增量草稿」完整重写为结构化周报（目标→决策→工程→成果→问题→局限→学到→计划），数字全部保留真实值。
- 提交 `83392b0`「week01: reorganize weekly log into structured report ...」（仅本地，push 留待行情满额里程碑一起推）。

---

## 本周待办清单（滚动更新）

1. ⏳ 续跑行情剩余 ~346 只 → 满 5553 全覆盖
2. ⏳ 行情满后：校验 → `git push all`（Gitee + GitHub，含 `83392b0`）→ 启动全市场财务 `--only fina`
3. ⏳ daily_basic 字段缺失问题写进周五整理版周报（已在本文件 9/7 节，整理时保留）
4. ⏳ pandas 版本债（venv 3.0.5 vs requirements <3.0），全量收尾前统一
5. ⏳ 调研资产负债表替代源（week01 遗留）
